export interface CustomerStat {
  customerId: number;
  firstName: string;
  lastName: string;
  appointmentCount: number;
}

export interface MonthStat {
  month: number;
  appointmentCount: number;
}
